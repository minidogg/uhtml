import path from 'path'
import fs from 'fs'

import { XMLParser, XMLBuilder, XMLValidator} from "fast-xml-parser"

export function BuildApp(appPath){
    const pagePath = path.join(appPath, "pages")
}